# Core ML/DL libraries
torch>=2.0.0
# transformers: 兼容旧版和新版参数名 (evaluation_strategy vs eval_strategy)
# Compatible with both old and new versions of transformers parameter names
transformers>=4.3.0
datasets>=2.12.0
accelerate>=0.20.0

# Data processing
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.2.0

# API
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# UI
streamlit>=1.24.0

# Utilities
tqdm>=4.65.0
